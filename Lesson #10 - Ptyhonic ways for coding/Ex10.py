# לפניך רשימה של שפות תכנות
# (כתבי קוד המחזיר רשימה שבה נמצאת רק השפה שאת לומדת (ע"י שימוש ב-filter ו- lambda

languages = ['HTML', 'Ruby', 'Python', 'Java']

#filter( lambda x : x == 'Python', languages )
#list (filter( lambda x : x == 'Python', languages ))
print(list (filter( lambda x : x == 'Python', languages )))